CREATE TABLE dublinBikesDockLocationEntity (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    latitude REAL NOT NULL,
    longitude REAL NOT NULL
);

selectAll:
SELECT * FROM dublinBikesDockLocationEntity;

insertOrReplace:
INSERT OR REPLACE INTO dublinBikesDockLocationEntity(
  id,
  name,
  latitude,
  longitude
)
VALUES (?, ?, ?, ?);

deleteAll:
DELETE FROM dublinBikesDockLocationEntity;
